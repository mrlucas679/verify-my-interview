/** @type {import('jest').Config} */
module.exports = {
  preset: 'ts-jest',
  testEnvironment: 'node',
  roots: ['<rootDir>/tests'],
  testMatch: ['**/*.test.ts'],
  // Full-pipeline evals make (gracefully failing) network attempts offline.
  testTimeout: 180000,
};
